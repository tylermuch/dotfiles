alias ctags="`brew --prefix`/bin/ctags"
    
ff() {
    FILE=`fzf $@`
    if [[ -e $FILE ]]; then
        echo $FILE
        echo -n $FILE | pbcopy
    fi
}

set rtp+=/usr/local/opt/fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
